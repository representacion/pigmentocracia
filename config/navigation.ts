import { Page } from "@/types/config/pages";

/**
 * @constant {Page[]} PAGES - The pages to display in the navigation elements.
 */
const PAGES: Page[] = [
    {
        title: "Inicio",
        id: "home",
        href: "/"
    }
];

export { PAGES };